name: Test Schedule
on:
  schedule:
    - cron: "*/40 * * * *"  # Every 10 minutes
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduled workflow working!"
